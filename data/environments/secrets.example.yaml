# Example contract only.
# Never store real secret values in version control.

required_secret_names:
  - APP_USERNAME
  - APP_PASSWORD
  - API_TOKEN

notes:
  - Secret values are supplied at runtime through environment variables or CI secret stores.
  - This file documents names only.
